---
# ansible-playbook main.yaml --ask-pass --extra-vars "domain=DEMO.FrogGenius.com project=PAT http_port=8080 https_port=8443"
- import_playbook: build-server.yaml
#- import_playbook: build-docker.yaml
#- import_playbook: setup-container.yaml

#- hosts: web
#  tasks:
#    - name: run create database script
#      command: bash /data/{{ project }}/build-database.sh {{ project }}


